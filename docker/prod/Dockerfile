# Production Dockerfile — uses COPY (not volume mounts).
# To be implemented when deploying.
#
# Key differences from dev:
#   - COPY ./server /var/www/html  (code baked into image)
#   - php-fpm instead of artisan serve
#   - No dev dependencies (composer install --no-dev)
#   - Nginx sidecar or separate service for static files
